// *    Tip calculator -> Lecture 24

/*
    let tip, billValue;
    billValue = prompt("Enter the value of bill");

    tip =
    billValue >= 50 && billValue <= 300
        ? (billValue / 100) * 15
        : (billValue / 100) * 20;

    console.log(
    `The bill values ${billValue}, the tip was ${tip} and the total value is ${
        Number(billValue) + Number(tip)
    } 😇`
    );

*/
